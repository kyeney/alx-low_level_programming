#include "main.h"
/**
 * a + b - adds two integers and returns the result.
 * @a: first number
 * @b: the second number
 * Return: the addition of both
 */

int add(int a, int b)
{
	return (a + b);
}
