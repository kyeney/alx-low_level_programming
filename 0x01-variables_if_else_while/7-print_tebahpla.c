#include <stdio.h>
/**
 * main -  Prints the lowercase alphabet in reverse
 *
 * Return: Always 0 (success)
 */

int main(void)
{
	char c;

	c = '2';
	while
		(c >= 'a') {
			putchar(c);
			c--;
		}
	putchar('\n');
	return (0);
}
